# Take one RGB frame on demand.  LLM-facing MCP tool.
# RPC — agent calls this to get a single image.
[contract]
id      = "beingbeyond/primitive/camera/snapshot"
version = "1"
kind    = "primitive"
idl     = "camera/srv/GetCameraImage.srv"

[mode]
type = "rpc"
