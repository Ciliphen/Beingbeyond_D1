# Set speed limit per joint.  NOT exposed as MCP — LLM should not tune
# motor parameters directly; skill/service set these in task context.
[contract]
id      = "beingbeyond/primitive/hand/set_speed"
version = "1"
kind    = "primitive"
idl     = "hand/srv/SetSpeed.srv"

[mode]
type = "rpc"
