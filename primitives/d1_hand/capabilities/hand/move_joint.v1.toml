# Set all joint positions.  LLM-facing MCP tool.
# Input:  float64[] positions  (0=open, 1=closed)
# Output: bool ok, string message
[contract]
id      = "beingbeyond/primitive/hand/move_joint"
version = "1"
kind    = "primitive"
idl     = "hand/srv/MoveJoint.srv"

[mode]
type = "rpc"
