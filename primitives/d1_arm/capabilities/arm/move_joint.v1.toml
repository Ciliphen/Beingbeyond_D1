# Set all joint positions in radians (absolute).  LLM-facing MCP tool.
# Input:  float64[] positions  (radians, absolute)
# Output: bool ok, string message
[contract]
id      = "beingbeyond/primitive/arm/move_joint"
version = "1"
kind    = "primitive"
idl     = "arm/srv/MoveJoint.srv"

[mode]
type = "rpc"
