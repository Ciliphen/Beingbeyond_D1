# Lifecycle entry point.  Every primitive MUST have this contract.
[contract]
id      = "beingbeyond/primitive/arm/driver"
version = "1"
kind    = "primitive"
idl     = "lifecycle/srv/Driver.srv"

[mode]
type = "rpc"
