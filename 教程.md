# 基于 YOLO 的立方体堆叠实战教程

本教程带你把本仓库的 **`block_grasp`** 模块从零跑起来：训练一个能识别彩色立方体的 YOLO OBB（旋转框）模型，完成手眼标定，验证坐标链路，最后让 D1 机械臂 + 灵巧手自动抓取并把一个方块**堆叠**到另一个之上。

整条链路是：

```
RealSense 相机取图
  → YOLO OBB 检测（中心 u,v + 旋转角 r）
  → 像素 → 桌面世界坐标（2D 单应 H）
  → scipy SLSQP 逆解 IK
  → 机械臂笛卡尔插值移动 + 灵巧手开合抓取
  → 堆叠到底座方块上方
```

> ⚠️ **安全第一**：真机操作全程把急停按钮放在手边。机械臂会实际运动，确保工作范围内无人、无障碍。首次运行请留足空间、低速观察。

阅读顺序建议按本文章节依次进行，每一步都有**验证方法**，通过后再进入下一步。

---

## 0. 目录与角色

关键文件（相对仓库根目录）：

| 路径 | 作用 |
| --- | --- |
| `lib/beingbeyond_d1_sdk-*.whl` | D1 SDK 预编译包（按 Python 版本选） |
| `object_detect/json2label.py` | LabelMe 标注 → YOLO OBB 标签 + 划分训练/验证集 |
| `object_detect/train.py` | YOLO OBB 训练脚本 |
| `object_detect/detect.py` | 检测推理函数（被抓取模块调用） |
| `block_grasp/calibrate_handeye.py` | 手眼标定（生成 `handeye_calib.npz`）**必做** |
| `block_grasp/test_calib.py` | 标定验证：点图看世界坐标 |
| `block_grasp/test_click_goto.py` | 标定验证：点图让指尖去 |
| `block_grasp/calibrate_sag.py` | 重力下垂补偿标定（可选） |
| `block_grasp/run_block_grasp.py` | 抓取入口脚本 |
| `block_grasp/config.py` | 所有可调参数 |

---

## 1. 环境创建

### 1.0 硬件要求

- BeingBeyond D1 机器人（头部 + 机械臂 + 灵巧手 + RealSense 相机）
- Linux PC（Ubuntu 20.04 / 22.04）
- USB 3.0 接口

本项目涉及**两个** conda 环境，分工明确，建议分开建：

- **`bb_d1`**：真机运行环境（相机、标定、抓取），需要装 D1 SDK wheel。
- **`bb_gpu`**：YOLO 训练环境（GPU、ultralytics），`object_detect/train.py` 顶部注释即用此环境。

如果你只有一台带 GPU 的机器且想省事，也可以只用一个环境把两边依赖都装上；但训练和真机通常在不同机器，分开更清晰。

### 1.1 运行环境 `bb_d1`

SDK wheel 区分 Python 版本，`lib/` 下有 cp38 和 cp310 两个。这里以 Python 3.10 为例（cp38 同理，把版本号换掉）：

```bash
conda create -n bb_d1 python=3.10 -y
conda activate bb_d1
pip install -U pip

# 安装 D1 SDK（提供 head_arm / dex_hand / pin_kinematics / urdf_path）
pip install lib/beingbeyond_d1_sdk-0.2.0-cp310-cp310-manylinux_2_17_x86_64.manylinux2014_x86_64.whl

# 抓取管线依赖
pip install ultralytics opencv-python scipy roboticstoolbox-python
pip install -r requirements.txt   # pyrealsense2
```

验证 SDK 装好了：

```bash
python -c "from beingbeyond_d1_sdk.head_arm import HeadArmRobot; \
           from beingbeyond_d1_sdk.dex_hand import DexHand; \
           from beingbeyond_d1_sdk.pin_kinematics import D1Kinematics; \
           print('SDK OK')"
```

### 1.2 训练环境 `bb_gpu`

训练不依赖 D1 SDK，只需要 ultralytics（会自带 PyTorch）：

```bash
conda create -n bb_gpu python=3.10 -y
conda activate bb_gpu
pip install -U pip
pip install ultralytics opencv-python
# GPU 训练需确保 torch 能用 CUDA：
python -c "import torch; print(torch.cuda.is_available())"   # 期望 True
```

### 1.3 串口权限（机械臂 / 头部）

机械臂头臂链走 USB 串口（默认 `/dev/ttyUSB0`）：

```bash
sudo apt remove brltty          # brltty 会误占串口
# 重新插拔 USB 线后：
ls /dev/ttyUSB*                 # 期望看到 /dev/ttyUSB0

# 若打开串口报权限错误 (Failed to open port)：
sudo usermod -aG dialout $USER
newgrp dialout                  # 或注销重登
```

### 1.4 硬件上电检查

1. 机器人上电，等待**灵巧手自动标定完成**（听到/看到手指归位）。
2. 急停按钮放在手边。
3. 若出现异常运动：按急停 → 停脚本 → 手动复位 → 松急停 → 重试。
4. 手指标定失败：按一次急停再松开，重试。

先用仓库自带示例确认三大件都通：

```bash
conda activate bb_d1
cd examples
python 1_control_hand.py        # 灵巧手
python 2_control_head_arm.py    # 头 + 臂
python 3_show_vision.py         # RealSense 图像
python 4_control_d1.py          # 全身运动（头+臂+手联动）
python 5_ik_control.py          # 末端位姿 IK 求解与执行
python 6_keyboard_teleop.py     # 键盘实时遥操作机械臂
```

> **注意**：`6_keyboard_teleop.py` 必须在**真实终端**内运行（Ubuntu Terminal / macOS Terminal），不建议在 VSCode、PyCharm、Jupyter 等 IDE 中运行，否则键盘原始输入模式无法工作。

<details>
<summary>6_keyboard_teleop.py 键位表（点击展开）</summary>

```
平移:
  w / s : X+ / X-
  a / d : Y+ / Y-
  z / x : Z+ / Z-

姿态:
  u / o : roll  + / -
  i / k : pitch + / -
  j / l : yaw   + / -

灵巧手:
  Space : hand_pos 在 0 与 1 之间切换

其他:
  r     : 重置末端目标与关节状态
  h     : 打印帮助
  q     : 退出
```
</details>

三个都正常，才进入下一步。

---

## 2. YOLO 模型训练

目标：训练一个 **YOLO11 OBB** 模型，输出每个立方体的**旋转框**（中心 + 宽高 + 旋转角）。旋转角很关键——抓取时用它对齐手腕，让手指沿方块边缘包住。

数据流：

```
采集图片 (test_detect_fixed.py, SPACE 存图)
  → LabelMe 画多边形标注 (*.json)
  → json2label.py 转 YOLO OBB 标签 + 划分 train/val
  → 写 data.yaml
  → train.py 训练
  → 产物 object_detect/runs/train*/weights/best.pt
```

### 2.1 采集数据

在 `bb_d1` 环境用采集脚本，它会把头部摆到**标定姿态**（yaw=-10°, pitch=35°，与后续手眼标定一致），把手臂抬离视野，然后实时显示相机画面：

```bash
conda activate bb_d1
cd ~/Beingbeyond_D1
python block_grasp/test_detect_fixed.py --no-head   # 无机械臂时加 --no-head 仅看相机
# 有真机时去掉 --no-head，脚本会摆好头部姿态
```

按键：

| 按键 | 作用 |
| --- | --- |
| `A`/`D` | 头部 yaw 左/右 |
| `W`/`S` | 头部 pitch 上/下 |
| `H` | 头部回到标定姿态 |
| `SPACE` | 保存当前帧到 `dataset/raw/`（自动编号，不覆盖） |
| `Q`/`ESC` | 退出 |

采集要点：

- **保持采集时的头部姿态与后续标定、抓取一致**（默认 yaw=-10°、pitch=35°）。视角变了检测坐标就对不上。
- 变换方块的位置、朝向、数量、光照，多拍一些。几十到一两百张即可起步，越多越稳。
- 图片存到 `dataset/raw/00000.jpg`、`00001.jpg` ……

### 2.2 标注（LabelMe）

用 [LabelMe](https://github.com/wkentaro/labelme) 打开 `dataset/raw/`，对每个方块画**多边形**（沿方块可见轮廓框一圈即可，`json2label.py` 会用 `cv2.minAreaRect` 拟合成最小旋转矩形）。

标签名**必须**是这 4 个之一（来自 `json2label.py` 的 `CLASS_NAMES`）：

```
red_cube   blue_cube   green_cube   yellow_cube
```

保存后每张图旁边会生成同名 `*.json`，与 `.jpg` 一起放在 `dataset/raw/`。

### 2.3 转换标签 + 划分数据集

```bash
python object_detect/json2label.py
```

它会：

- 读取 `dataset/raw/*.jpg + *.json`；
- 转成 YOLO OBB 标签（8 个归一化角点坐标）；
- 按 8:2 随机划分（固定随机种子 42，可复现）；
- 输出到 `object_detect/dataset/{images,labels}/{train,val}/`；
- 在 `object_detect/dataset/labels_preview/` 生成可视化图，**务必打开检查**标注框对不对。

### 2.4 写 `data.yaml`

复制模板并按实际类别改。**注意**：仓库里的 `object_detect/data.yaml.example` 是旧的（3 类、命名是 `*_block`），**不要照抄**。类别名和顺序必须与 `json2label.py` 的 `CLASS_NAMES` 完全一致，否则训练出的模型类名对不上 `config.py` 里的 `PLACE_POSITIONS`。

创建 `object_detect/data.yaml`：

```yaml
path: $HOME/Beingbeyond_D1/object_detect/dataset
train: images/train
val: images/val

names:
  0: red_cube
  1: blue_cube
  2: green_cube
  3: yellow_cube
nc: 4
```

> 只用到部分颜色也没关系，但 `names` 的**索引顺序**必须和 `json2label.py` 的 `CLASS_NAMES` 一致（`red=0, blue=1, green=2, yellow=3`）。

### 2.5 训练

```bash
conda activate bb_gpu
cd ~/Beingbeyond_D1
python object_detect/train.py
```

`train.py` 默认：基座模型 `yolo11x-obb.pt`（首次自动下载约 56 MB）、`imgsz=640`、`epochs=1000`、`batch=8`、`device="0"`（第一块 GPU）、`optimizer="SGD"`。数据集小的话可以把 `epochs` 调小（比如 200），或换更小的基座模型（`yolo11n-obb.pt` / `yolo11s-obb.pt`）加快训练。

产物在 `object_detect/runs/train/weights/best.pt`（重复训练会生成 `train2/`、`train3/` ……）。

> **抓取脚本会自动选取 `object_detect/runs/` 下最新的 `best.pt`**，所以一般不用手动指定。也可用 `--model 路径` 显式指定。

### 2.6 离线验证模型

不接机械臂，先拿一张图快速看看模型识别得对不对：

```bash
conda activate bb_d1
python block_grasp/test_detect_image.py   # 对已有图片跑检测，画出旋转框和颜色
```

或接相机实时看（复用 2.1 的脚本，它同时在跑检测）：

```bash
python block_grasp/test_detect_fixed.py --model object_detect/runs/train/weights/best.pt
```

画面里方块被稳定框住、颜色标对，即可进入标定。若识别不稳，回到 2.1 补数据重训，或临时调低 `config.py` 的 `CONF_THRESHOLD`。

---

## 3. 手眼标定

抓取**依赖标定**，这是把「像素坐标」映射到「机械臂基座下的桌面坐标」的关键一步。本项目用的是 **2D 单应（homography）**：假设方块都放在同一张平桌上，用一个 3×3 矩阵 `H` 把像素 `(u,v)` 直接映到桌面 `(x,y)`。

原理：采集若干组「像素点 ↔ 该点在机械臂基座系下的真实 XY」配对，最小二乘解出 `H`，同时拟合桌面平面 `z = a·x + b·y + c`。

```bash
conda activate bb_d1
cd ~/Beingbeyond_D1
python block_grasp/calibrate_handeye.py
```

脚本启动后会：摆到安全位姿 → 把头部固定到 yaw=-10°/pitch=35°（**标定期间头绝对不能动**，这个角度会被存进标定文件，抓取时必须复现）→ 打开相机窗口。

### 操作流程

在弹出的 OpenCV 窗口里（按键在窗口内响应）：

1. **点桌面上一个参考点**（画面出现绿色十字）。选特征清楚、好对准的点，比如贴一小块标记或桌面纹理角。
2. **把指尖移到该物理点正上方并贴到桌面**，用键盘遥操：
   - `W/S` = X±（前/后）， `A/D` = Y±（左/右）， `Z/X` = Z±（上/下）
   - `U/O` `I/K` `J/L` = 姿态微调（roll/pitch/yaw）
   - `B` = 开合手（有时要收手才好对准）， `R` = 复位末端
3. **按 `SPACE`** 记录一组 (像素, 世界) 配对。
4. 换位置重复，**至少 4 组，建议 6~10 组，分散铺满整个工作区**（近/远、左/右都要有）。
5. **按 `C`** 计算并保存。终端会打印 XY 误差和 Z 误差（mm）。**平均 XY 误差 < 20 mm 才会自动保存**为 `block_grasp/handeye_calib.npz`；否则提示你多加点或重来。

对准技巧：让指尖真正落在你点的那个物理点上，误差主要来自这一步。分散、准确的配对点比数量更重要。

保存的 `handeye_calib.npz` 含：单应矩阵 `H`、桌面平面参数、标定时的头部 yaw/pitch、所有配对点、平均误差。

---

## 4. 标定验证

标定完**别急着抓**，先验证坐标链路。有两个脚本，从易到难：

### 4.1 只验证坐标换算（不动手臂，安全）

```bash
python block_grasp/test_calib.py
```

它会把头摆到标定姿态，然后你在图上**点一个点**，终端打印该像素经 `H` 算出的世界坐标 `(x, y)`。拿尺子量一下点的实际位置，和打印值对比，误差应在毫米级（和标定误差一致）。

### 4.2 验证整条链路（会动手臂）

```bash
python block_grasp/test_click_goto.py
```

在图上**点一个点，机械臂指尖就会移动到那个位置**。这一步同时验证了「像素→世界坐标→IK→执行」整条链路。指尖落点和你点的位置吻合，说明标定和 IK 都 OK。

> 若整体偏移：重做手眼标定，并确认头部姿态确实是 `HEAD_YAW_DEG=-10`、`HEAD_PITCH_DEG=35`。
> 若偏高/偏低：调 `config.py` 的 `GRASP_Z_OFFSET`；远处系统性偏低可做第 5 节的下垂标定。

---

## 5. 重力下垂标定（可选）

机械臂伸得越远，自重导致末端越往下垂。若你发现**远处方块总抓不到（偏低）**，做这一步；否则可跳过。

模型：`下垂量 dz = factor · dist³`（`dist` 为水平径向距离）。脚本会在关闭补偿的情况下，把末端以抓取姿态移动到几个不同距离、下降到名义抓取高度并停住，让你**用尺子量末端到桌面的实际高度**，最后拟合出 `factor`。

```bash
conda activate bb_d1
python block_grasp/calibrate_sag.py
# 从远到近遍历、对比机械背隙迟滞：python block_grasp/calibrate_sag.py --reverse
```

按提示：每停一个点，量**同一个末端参考点**（比如某根指尖）到桌面的垂直距离（mm）并输入，回车跳过。测 ≥2 个点后脚本打印建议的 `GRAVITY_SAG_FACTOR` 和拟合 R²。把这个值填回 `block_grasp/config.py`：

```python
GRAVITY_SAG_FACTOR: float = 0.3   # ← 改成脚本建议值
```

---

## 6. 运行抓取

前面都通过后，就能跑主流程了。默认开启**堆叠模式**（`STACK_ENABLED=True`）：检测到 ≥2 个方块时，以离 `STACK_POSITION`（默认 `[0.25, 0, 0.105]`）最近的方块为**底座**，把另一个方块叠到它正上方，完成后停止；按 `R` 重置重来。

```bash
conda activate bb_d1
cd ~/Beingbeyond_D1

python block_grasp/run_block_grasp.py            # 手动模式：SPACE 触发一次抓取
python block_grasp/run_block_grasp.py --auto     # 自动模式：连续检测并抓取
python block_grasp/run_block_grasp.py --headless # 无显示窗口
```

常用参数：

| 参数 | 说明 |
| --- | --- |
| `--auto` | 自动抓取，不等按键 |
| `--headless` | 不开显示窗口 |
| `--model PATH` | 指定 YOLO `.pt`（省略则自动选 `runs/` 下最新 `best.pt`） |
| `--hand-type {right,left}` | 灵巧手左右手，默认 `right` |
| `--hand-can IFACE` | 灵巧手 CAN 接口，默认 `can0` |
| `--arm-dev DEV` | 机械臂串口，默认 `/dev/ttyUSB0` |
| `--cam-width/--cam-height` | 相机分辨率，**必须与标定时一致**（默认 1280×720） |

运行时窗口内按键：

| 按键 | 作用 |
| --- | --- |
| `SPACE` | 触发一次抓取（手动模式） |
| `A` | 切换自动/手动 |
| `R` | 重置堆叠状态 |
| `ESC`/`Q` | 退出 |

**建议首次运行用手动模式**（不带 `--auto`），摆一个方块，按一次 `SPACE` 观察整个抓取动作是否安全平稳，确认无误再放两个方块试堆叠、再开 `--auto`。

---

## 7. 关键参数调优（`block_grasp/config.py`）

抓取效果几乎都靠这些参数微调。按现象对照调：

| 参数 | 默认 | 何时调 |
| --- | --- | --- |
| `HEAD_YAW_DEG / HEAD_PITCH_DEG` | -10 / 35 | 必须与手眼标定时一致，改了要重标定 |
| `CONF_THRESHOLD` | 0.85 | 检测漏识别 → 调低；误检多 → 调高 |
| `BLOCK_SIZE` | 0.05 | 方块边长（m），换尺寸要改 |
| `GRASP_Z_OFFSET` | 0.015 | 抓取高度：偏高调小、偏低调大（可为负） |
| `GRAVITY_SAG_FACTOR` | 0.3 | 远处偏低 → 由第 5 节标定写回 |
| `HAND_OPEN/GRASP/CLOSE` | 见文件 | 手指开合幅度：抓空/夹不住时调 `HAND_GRASP` |
| `GRASP_OFFSET_X/Y` | 0 / 0 | 抓取点系统性偏移的微调（世界系 XY，m） |
| `GRASP_YAW_OFFSET_DEG` | 0 | 拇指没包住方块 → 试 ±90 |
| `EE_ROLL/PITCH/YAW_DEG` | 178/61/-175 | 手掌相对桌面的朝向 |
| `STACK_ENABLED` | True | 关掉则按类别摆到 `PLACE_POSITIONS` |
| `STACK_POSITION` | [0.25,0,0.105] | 堆叠塔底座位置（选离它最近的方块当底座） |
| `PLACE_POSITIONS` | 见文件 | 非堆叠模式下每种颜色的摆放位（按实测桌高调 z） |
| `INTERP_STEP_SIZE` | 0.025 | 移动步长（m），越大越快、轨迹越粗 |
| `CATCH_DELAY_S` | 0.8 | 手开合后的等待（CAN 延时） |

`z` 高度类参数（`PLACE_POSITIONS`、`STACK_POSITION`、`DEFAULT_PLACE_Z`）都基于**你的实际桌面高度** + 半个方块，务必按真机量一次再调。

---

## 8. 故障排查

| 现象 | 排查方向 |
| --- | --- |
| 检测不到方块 | 确认权重路径、光照；调低 `CONF_THRESHOLD`；补数据重训 |
| 颜色/类别识别错 | `data.yaml` 的 `names` 顺序须与 `json2label.py` 的 `CLASS_NAMES` 一致 |
| 抓取位置整体偏移 | 重做手眼标定；确认头部姿态为 `HEAD_YAW_DEG`/`HEAD_PITCH_DEG`；确认相机分辨率与标定一致 |
| 抓取偏高/偏低 | 调 `GRASP_Z_OFFSET`；远处偏低启用下垂标定 |
| 抓空 / 夹不住 | 调 `HAND_GRASP`；检查 `BLOCK_SIZE` 与实际方块是否一致 |
| 拇指撞到方块 | 调 `GRASP_YAW_OFFSET_DEG`（试 ±90） |
| IK 报无解 / 不动 | 目标超出工作空间或关节限位；把方块挪近，或换抓取角度 |
| 关节突然大幅跳动被拦截 | 触发 `JOINT_JUMP_THR_DEG`（20°）近奇异保护，检查 IK 解是否连续 |
| 串口打不开 | 见 1.3：`dialout` 组权限、移除 brltty |
| 相机打不开 | RealSense 需 USB 3.0，检查是否插在 USB 2.0 口上 |
| 手指错位 | 断电重启，等灵巧手自动标定完成 |

---

## 附：最短跑通清单

```bash
# 1. 环境
conda activate bb_d1                        # 运行环境（已装 SDK wheel + requirements）

# 2. 训练（在 bb_gpu 环境，GPU 机器上）
#    采集 → LabelMe 标注 → 转换 → 训练
python block_grasp/test_detect_fixed.py     # 采集，SPACE 存图到 dataset/raw/
python object_detect/json2label.py          # 转标签 + 划分
# 编辑 object_detect/data.yaml（4 类，*_cube）
conda activate bb_gpu && python object_detect/train.py

# 3. 手眼标定（bb_d1）
conda activate bb_d1
python block_grasp/calibrate_handeye.py     # 点图→对指尖→SPACE，6+ 组，C 保存

# 4. 验证
python block_grasp/test_calib.py            # 点图看坐标（不动臂）
python block_grasp/test_click_goto.py       # 点图对指尖（动臂）

# 5. （可选）下垂标定
python block_grasp/calibrate_sag.py

# 6. 抓取
python block_grasp/run_block_grasp.py       # 手动，SPACE 触发
python block_grasp/run_block_grasp.py --auto
```
</content>
</invoke>
